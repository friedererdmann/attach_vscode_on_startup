{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "wait_for_port",
            "type": "shell",
            "windows":{
                "command": "${workspaceFolder}/check_port.bat",
                "args": ["7740"]
            }
        },
    ]
}